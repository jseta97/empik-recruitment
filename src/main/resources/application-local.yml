spring:
  datasource:
    url: 'jdbc:h2:file:./users'
    username: 'empik'
    password: 'empik'
  h2:
    console:
      enabled: true
  jpa:
    hibernate:
      ddl-auto: update
  flyway:
    baseline-on-migrate: true
